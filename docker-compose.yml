include:
  - db-service.yml

services:
  client:
    build: 
      context: ./client
      dockerfile: Dockerfile
    image: "recipe-box.client:latest"
    ports:
      - "8080:80"
    networks: 
      - teapothill
    depends_on:
      - api

  api:
    build:
      context: ./api
      dockerfile: Dockerfile.production
    image: "recipe-box.api:latest"
    ports:
      - "8181:80"
    networks:
      - teapothill
    env_file:
      - path: "./config/.bills_galley.env"
        required: true
    environment:
      - RAILS_MASTER_KEY=${RAILS_MASTER_KEY}
    depends_on:
     db:
       condition: service_healthy
 
volumes:
  teapothill_volume:
    external: true

networks:
  teapothill:
    external: true
